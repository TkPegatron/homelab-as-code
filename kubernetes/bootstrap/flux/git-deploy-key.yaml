# yamllint disable
# https://github.com/k8s-at-home/template-cluster-k3s/issues/324
apiVersion: v1
kind: Secret
metadata:
    name: github-deploy-key
    namespace: flux-system
stringData:
    identity: {{ PRIVATE_SSH_KEY }}
    known_hosts: {{ GIT_SERVER_SSH_PUBLIC_KEY }}
