---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: actual-data
  namespace: default
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
