#shellcheck disable=SC2148,SC2155
export KUBECONFIG=$(expand_path ./private_keys/kubeconfig)
export SOPS_AGE_KEY_FILE=$(expand_path ~/.config/sops/age/keys.txt)

# -{Cluster tool aliases}
alias ceph='kubectl exec -it -n rook-ceph deployments/rook-ceph-tools -- ceph'